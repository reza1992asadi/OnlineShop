import './cart-item.styles.scss' 

const CartItem = (Itemdata) => { 
    const [name, image, price, quantity] = Itemdata ; 
        return (
            <div>
                Hello ; 
            </div>
        ); 
}

export default CartItem ; 